# stage2 SFT deps — verified co-installable stack (unsloth is the gating dep).
# NOTE: setup.sh installs torch==2.6.0 + xformers==0.0.29.post3 from the cu124
# index BEFORE this file. Do NOT float transformers (unsloth has a long
# exclusion list) and keep huggingface_hub on the 0.35.x band (transformers
# <1.0 ceiling AND unsloth >=0.34.0 floor). Bump deliberately.
# NOTE (router-load fix, RESOLVED): two Stage-2 runs crashed at load with
# "Unsloth: Critical error since some weights are not initialized" — gpt-oss's
# per-layer mlp.router.{weight,bias} keys go unmapped under Unsloth's patched
# GptOss loader (bumping unsloth 2026.7.3 -> 2026.7.4 fixed the *patching* step
# but NOT the weight load). Fix taken = fallback (b): gpt-oss now trains via
# PLAIN transformers + bitsandbytes + PEFT (shared/train_common._load_plain_peft),
# which loads the abliterated checkpoint fine (the eval path already proved it).
# unsloth stays installed — dense (qwen) families still use its faster loader,
# and gpt-oss still uses unsloth.chat_templates.train_on_responses_only for
# response-only masking. bitsandbytes/peft/accelerate below carry the plain path.
unsloth==2026.7.4
unsloth_zoo==2026.7.4
trl==0.24.0
transformers==4.56.2
datasets==4.3.0
huggingface_hub==0.35.3
peft==0.19.1
accelerate==1.14.0
bitsandbytes==0.49.2
lm_eval==0.4.12
hf-transfer==0.1.9
swebench==4.1.0
# optional (KOMPRESS_COMPRESS=1): headroom[ml]  # kompress-v8 tool-output compression
