# stage5 RLVR deps — same verified stack as stage3 as the training base.
# setup.sh installs torch==2.7.1 + xformers==0.0.31.post1 from the cu126 index
# BEFORE this file. Do NOT float transformers; keep huggingface_hub on 0.35.x.
unsloth==2026.7.3
unsloth_zoo==2026.7.3
trl==0.24.0
transformers==4.56.2
datasets==4.3.0
huggingface_hub==0.35.3
peft==0.19.1
accelerate==1.14.0
bitsandbytes==0.49.2
lm_eval==0.4.12
hf-transfer==0.1.9
swebench==4.1.0
# TODO(research): pin the RL + exec-sandbox deps for GRPO/GSPO RLVR — TRL GRPO
# (GSPO via importance_sampling_level="sequence"; verify gpt-oss MoE support),
# vLLM (colocated group rollouts), and the hardened sandbox lib. Also evaluate
# `verifiers`+`prime-rl` (github.com/PrimeIntellect-ai/verifiers) instead of a
# hand-rolled reward/rollout. Versions finalized from SOTA research — do NOT
# guess. See docs/superpowers/plans/2026-07-19-extended-pipeline-*.md.
# trl==<pin with GSPO>
# vllm==<pin>
# verifiers==<pin>
# <sandbox-lib>==<pin>
