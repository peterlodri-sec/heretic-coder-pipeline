# stage4 RFT deps — same verified stack as stage3 (reuses stage2's SFT trainer).
# setup.sh installs torch==2.7.1 + xformers==0.0.31.post1 from the cu126 index
# BEFORE this file. Do NOT float transformers; keep huggingface_hub on 0.35.x.
unsloth==2026.7.3
unsloth_zoo==2026.7.3
trl==0.24.0
transformers==4.56.2
datasets==4.3.0
huggingface_hub==0.35.3
peft==0.19.1
accelerate==1.14.0
bitsandbytes==0.49.2
lm_eval==0.4.12
hf-transfer==0.1.9
swebench==4.1.0
# TODO(research): pin the candidate-generation + exec-sandbox deps for the RFT
# loop — vLLM (batched candidate sampling) and the hardened sandbox lib
# (Docker/nsjail/Firejail or verifiers). Versions finalized from SOTA research —
# do NOT guess. See docs/superpowers/plans/2026-07-19-extended-pipeline-*.md.
# vllm==<pin>
# <sandbox-lib>==<pin>
